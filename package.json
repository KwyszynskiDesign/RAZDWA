{
  "devDependencies": {
    "esbuild": "^0.27.3",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  },
  "name": "razdwa",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild src/ui/main.ts --bundle --outfile=docs/assets/app.js --platform=browser --target=es2019 --minify",
    "test": "vitest run"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1"
  }
}
