{
  "devDependencies": {
    "@types/node": "^25.5.0",
    "esbuild": "^0.27.3",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  },
  "name": "razdwa",
  "version": "1.0.0",
  "scripts": {
    "prebuild": "node scripts/inject-cache-version.js",
    "build": "npm run prebuild && esbuild src/ui/main.ts --bundle --outfile=docs/assets/app.js --platform=browser --target=es2019 --minify --charset=ascii",
    "build:commit": "npm run build && git add docs/assets/app.js sw.js docs/sw.js && git commit -m \"build: update app.js and service worker cache version\"",
    "push:main": "git pull --rebase origin main && git push origin main --force-with-lease",
    "sync": "npm run build:commit && npm run push:main",
    "dev": "npx http-server docs -p 8080 -o",
    "test": "node scripts/run-vitest.mjs"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1",
    "zod": "^4.3.6"
  }
}
