{
  "name": "pricing-calculator",
  "version": "1.0.0",
  "description": "Price calculator foundation",
  "main": "src/ui/main.ts",
  "scripts": {
    "test": "vitest run",
    "build": "esbuild src/ui/main.ts --bundle --outfile=public/assets/app.js --platform=browser --format=esm",
    "dev": "esbuild src/ui/main.ts --bundle --outfile=public/assets/app.js --platform=browser --format=esm --watch"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.27.2",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
