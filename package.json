{
  "name": "razdwa-calculator",
  "version": "1.0.0",
  "description": "Kalkulator cenników RAZ DWA",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "build": "tsc && esbuild src/ui/main.ts --bundle --outfile=docs/assets/app.js --format=iife"
  },
  "devDependencies": {
    "esbuild": "^0.20.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3",
    "vitest": "^1.6.1"
  }
}
