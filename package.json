{
  "name": "pricing-calculator",
  "version": "1.0.0",
  "description": "Price calculator foundation",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "calculate": "ts-node src/index.ts",
    "build": "esbuild src/index.ts --bundle --outfile=public/assets/app.js --platform=browser --format=esm"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.27.2",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
