console.log("Kalkulator UI initialized");

// Initial placeholder for UI logic migration
document.addEventListener("DOMContentLoaded", () => {
    const appHeader = document.querySelector("h1");
    if (appHeader) {
        console.log("App header found:", appHeader.textContent);
    }
});
