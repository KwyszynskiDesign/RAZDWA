/**
 * /src/core/computeShipping.ts
 * Computes shipping cost. Currently all orders are collected in-store,
 * so this function returns 0.
 */

export function computeShipping(_price: number): number {
  return 0;
}
